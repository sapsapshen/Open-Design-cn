# 把 Open Design 带回中国使用场景：一次真正有用的本土化优化

这段时间，AI 设计工具很多，但真正能在国内开发环境里顺手跑起来的，并不多。很多项目看起来很强：能生成网页、能做 PPT、能出原型，甚至还能接各种模型和 Agent。可一到真正上手时，问题就来了：模型不好配、CLI 依赖太重、启动命令太长、页面地址找不到、预览还容易空白，最后用户的第一感受不是“好强”，而是“好麻烦”。

而这正是我们这次改造 Open Design 的出发点。

Open Design 本身来自开源项目 [nexu-io/open-design](https://github.com/nexu-io/open-design)。它的核心思路非常好：本地优先、Web 界面 + 本地 daemon、用 Skills 和 Design Systems 驱动设计生成，把 AI 从“聊天回答”推进到“直接产出设计文件”。从理念上看，它几乎就是一个面向未来的设计工作台。

但上游版本更像是为全球开发者准备的“能力底座”，并不完全适合中国本地的实际网络和使用习惯。于是，我们做了一个方向很明确的版本：不是推倒重来，而是围绕国内用户最容易卡住的几个点，把它改成一个更容易配置、更容易启动、更容易真正用起来的版本。

![项目生成预览](file.png)

首先，最关键的一步，是把“模型接入门槛”降下来。

很多类似工具默认建立在海外 CLI 或海外模型服务之上，第一次使用时，用户往往要先装一堆命令行工具，再配认证、再配环境变量。对于习惯本地开发的工程师来说，这都不算轻松；对于只想快速试一下 AI 设计能力的产品经理、设计师或独立开发者来说，几乎就是第一道劝退门槛。

这次中国优化版直接把默认入口切换到了更符合国内使用习惯的 API 模式：DeepSeek 作为对话与代码生成入口，SiliconFlow 作为 OpenAI-compatible 的模型入口。更重要的是，不再要求用户自己翻文档、记地址、拼参数，而是把首次配置做成了向导式流程。打开页面后，跟着步骤填入 Key，点一下 “Test connection”，能不能用当场就知道，成功后自动写入本地配置。

这件事看起来很小，但实际意义很大：从“你先研究半天怎么接上模型”，变成“你先把设计跑起来”。

![DeepSeek 配置向导](deepseek.png)

![SiliconFlow 配置向导](siliconflow.png)

第二个改动，是把“启动难度”降下来。

原本很多本地项目最典型的问题就是：命令能跑，但用户不知道该怎么跑；服务起来了，但不知道该打开哪个地址；甚至脚本本身在不同 shell 下还会踩坑。为了解决这个问题，我们直接补上了 macOS 可双击的启动和停止脚本，把常见流程收进 `start-open-design.command` 和 `stop-open-design.command`。用户不用再手打一长串命令，也不用自己猜端口。

现在这个版本把 Web 端和 daemon 端的地址固定下来：Web 默认就是 `http://127.0.0.1:17573/`，daemon 默认是 `http://127.0.0.1:17456`。脚本还会自动检查 `node`、`pnpm` 和 `node_modules`，缺依赖时先补安装，再启动服务。对于本地使用来说，这比“能力更多”更重要，因为它解决的是最先发生的真实问题：能不能顺利打开第一页。

第三个改动，是让“生成结果真的能落地”。

这次优化里，我们还修了一些特别影响体验、但往往容易被忽略的问题。比如模型有时会输出 fenced HTML 代码块，结果页面没法正确落盘，右侧预览就会是空白；再比如高保真 PPT 生成时，模型会偷懒塞进黑色或灰色占位块，看上去像做完了，其实只是把“缺图”藏了起来。

这些问题不属于“模型不够聪明”，而属于“产品没有帮用户兜底”。所以我们在这个版本里补上了两层防线：一层是 HTML artifact 的恢复与校验，避免生成内容明明存在却无法预览；另一层是对高保真结果里的视觉占位块进行拦截，不让“黑色方块”冒充最终交付物。也就是说，这个版本不只是更容易启动，也更重视“生成出来的东西是不是能直接看、直接改、直接用”。

![优化后的 PPT 预览](file1.png)

再往下看，这次优化其实还有一个很实用的方向：适配国内网络环境。

我们把部分生成物依赖从 `unpkg.com` 调整到 `unpkg.zhimg.com`，尽量减少因为 CDN 不可达导致的前端资源加载失败；品牌和页签也统一调整为 `Open Design - Chinese Optimized Version`，让用户更明确地知道自己正在使用的是一个经过本土化处理的版本，而不是原始上游分支。很多人会低估这种“细枝末节”的价值，但真正的易用性，恰恰就是由这些细节一点点堆出来的。

说到底，这个项目最想证明的一件事是：开源 AI 工具的价值，不只是“我也能做一个”，而是“我能把它改到适合我所在的真实环境”。Open Design 给了一个很强的基础，而中国优化版做的，是把它从“技术上可以运行”，推进到“本地用户真的愿意打开、真的能顺利配置、真的能持续使用”。

如果说上游 Open Design 是一个很好的设计引擎，那么这次本土化优化，做的就是把引擎装进一辆在中国路况里更好开的车。对于想把 AI 设计工作流落到本地、又不想被复杂配置拖住的人来说，这一步，可能比多接几个模型都更重要。
